spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

app:
  gateway:
    base-url: https://test.gateway.example.com
    timeout: 5000
    api-key: test-key
