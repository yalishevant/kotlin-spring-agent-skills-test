CREATE TABLE orders (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    customer_name VARCHAR(120) NOT NULL,
    status VARCHAR(32) NOT NULL,
    shipping_address VARCHAR(255) NOT NULL,
    customer_reference VARCHAR(80),
    notes VARCHAR(255),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE TABLE order_lines (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    order_id BIGINT NOT NULL,
    sku VARCHAR(64) NOT NULL,
    quantity INT NOT NULL,
    CONSTRAINT fk_order_lines_order FOREIGN KEY (order_id) REFERENCES orders(id)
);

CREATE TABLE audit_logs (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    order_id BIGINT NOT NULL,
    action VARCHAR(64) NOT NULL,
    status VARCHAR(32) NOT NULL,
    details VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE TABLE outbox_events (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    aggregate_type VARCHAR(64) NOT NULL,
    aggregate_id BIGINT NOT NULL,
    event_type VARCHAR(64) NOT NULL,
    payload CLOB NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);

INSERT INTO orders (customer_name, status, shipping_address, customer_reference, notes) VALUES
    ('Alice Buyer', 'DRAFT', 'Dock 1', 'REF-001', 'fragile'),
    ('Ben Receiver', 'CONFIRMED', 'North Hub', 'REF-002', NULL),
    ('Cara Ops', 'FULFILLING', 'Canal Street 9', NULL, 'priority');

INSERT INTO order_lines (order_id, sku, quantity) VALUES
    (1, 'SKU-CHAIR', 2),
    (1, 'SKU-LAMP', 1),
    (2, 'SKU-DESK', 1),
    (3, 'SKU-CABLE', 4);
